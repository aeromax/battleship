{
  "name": "battleship",
  "version": "1.0.0",
  "description": "",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "build:css": "sass src/styles.scss public/styles.css --no-source-map",
    "watch:css": "sass --watch src/styles.scss:public/styles.css --no-source-map",
    "server:dev": "nodemon --watch server --exec \"NODE_ENV=development node server/index.js\"",
    "dev": "npm-run-all build:css --parallel watch:css server:dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "npm-run-all": "^4.1.5",
    "sass": "^1.97.2"
  }
}
